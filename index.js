const findAns = (arr, expectedAns) => {
  const findMax = (anArr) => {
    let max;
    for (let i=1; i < anArr.length; i++){
      if(anAnArr[i] > anArr[0])
    }
  }
};
const expectedAns = 10;
const inputArr = [6, 3, 4];
console.log(findAns(inputArr, expectedAns));
